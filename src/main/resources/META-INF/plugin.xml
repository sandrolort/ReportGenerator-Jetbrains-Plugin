<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>org.kvisha.ReportGenerator</id>

    <name>ReportGenerator</name>

    <vendor email="lortkipanidze.sandro@proton.me" url="https://www.kvisha.org">Sandro Lortkipanidze</vendor>

    <description>
        Plugin that integrates the ReportGenerator functionality into Jetbrains IDE as a tool window.

        "ReportGenerator converts coverage reports generated by coverlet, OpenCover, dotCover, Visual Studio, NCover, Cobertura, JaCoCo, Clover, gcov, or lcov into human readable reports in various formats.
        The reports show the coverage quotas and also visualize which lines of your source code have been covered."

        <![CDATA[
            <ul>
            <li>Install the global CLI tool by running `dotnet tool install -g dotnet-reportgenerator-globaltool' before using the plugin.</li>
            <li>Install coverlet/coverlet.msbuild package in your test project.</li>
            <li>Click "Generate Report" button in the tool window. This will automatically run the tests and display the result.</li>
        ]]>

        The plugin currently only supports one test project at a time. Subject to change in future versions.
    </description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Report Generator" factoryClass="org.kvisha.reportgenerator.view.HtmlReportPlugin" anchor="right" icon="com.intellij.icons.ExpUiIcons.Run.ShowPassed"/>
        <postStartupActivity implementation="org.kvisha.reportgenerator.MyStartupActivity"/>
        <projectConfigurable instance="org.kvisha.reportgenerator.settings.ReportGeneratorSettingsConfigurable"
                             id="ReportGeneratorSettings"
                             displayName="Report Generator Settings"
                             groupId="tools"/>
    </extensions>

</idea-plugin>